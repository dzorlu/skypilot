
gpu: "A100-80GB:8"

secrets:
  - wandb-secret
  - gcp-credentials

image:
  base: "pytorch/pytorch:2.8.0-cuda12.6-cudnn9-devel"
  apt_packages:
    - git
    - tmux
    - htop
    - curl
  pip_packages:
    - transformers
    - datasets
    - huggingface-hub
    - wandb
    - google-cloud-storage

timeout: 86400
